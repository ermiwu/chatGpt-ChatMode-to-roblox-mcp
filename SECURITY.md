# 安全说明

本项目把 Roblox Studio 的读写能力暴露给经过 OAuth 授权的远程 MCP 客户端。仅在受信任的个人电脑与 ChatGPT 账号上使用。

请勿提交或分享 `.env.local`、Cloudflare 凭据、OAuth 密码、访问令牌、刷新令牌及日志中的敏感内容。服务仅监听回环地址；不要把 `REMOTE_HOST` 改为局域网或公网地址。执行写操作前，应在 Roblox Studio 保存或建立版本，并核对工具名称、目标与参数。

发现漏洞时，请通过私有渠道联系仓库所有者，不要在公开 Issue 中附带可用凭据或完整日志。报告应包含受影响版本、复现步骤、预期与实际行为，并使用已脱敏的示例。
